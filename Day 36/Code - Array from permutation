class Solution {
    public int[] buildArray(int[] nums) {
        int[] arr2=new int[nums.length];
        for(int i=0;i<nums.length;i++)
        {
            arr2[i]=nums[nums[i]];
        }
        return arr2;
    }
}
